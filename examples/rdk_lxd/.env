VERSION=v3
# GHCR OCI artifact holding the RDK rootfs (published once via `oras push`; see README)
RDK_ROOTFS_REF=ghcr.io/ketantewari/raikou/rdk-rootfs:qemux86broadband
# Attach virtual wlan radios to the CPE (1=on, 0=off)
ENABLE_WIFI=1
